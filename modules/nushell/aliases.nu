alias l = ls
alias ll = ls -l
alias la = ls -a

alias cat = bat
alias c = bat

alias g = git
alias gs = git status
alias ga = git add

alias n = nix
alias nfu = nix flake update
alias nfs = nix flake show
alias nb = nix build
alias nd = nix develop
alias ns = nix shell

alias e = hx

def mem [] { sys mem }
def df [] { sys disks }
