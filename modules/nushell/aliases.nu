alias g = git

alias n = nix
alias nfu = nix flake update
alias nfs = nix flake show
alias nb = nix build
alias nd = nix develop
alias ns = nix shell

alias e = hx

def mem [] { sys mem }
def df [] { sys disks }
